---
release type: minor
---

Expose `extend=True` on `strawberry_django.input()` and
`strawberry_django.partial()` so Django input types can extend existing GraphQL
input object types when used with Strawberry core input extension support.
